import { describe, it } from "vitest";

describe("getTaskHandler", () => {
  it("returns tasks", async () => {});
  it("throws if task key is non existent", async () => {});
});

describe("dynamicTaskHandlerImport", () => {
  it("randomises keys", async () => {});
  it("appends tasks from file to task handlers", async () => {});
  it("returns a task handler", async () => {});
});
